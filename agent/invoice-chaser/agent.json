{
  "schemaVersion": 1,
  "kind": "kavela.agent",
  "exportedAt": "2026-07-18T14:00:04.780Z",
  "sourceSlug": "invoice-chaser",
  "sourceVersion": "1.0.1",
  "portableConfig": {
    "agent": {
      "name": "Invoice Chaser",
      "description": "",
      "shortDescription": "Politely chases overdue invoices and sends reminders.",
      "defaultModel": null,
      "allowedModels": null,
      "searchConfig": {
        "max_results": 5,
        "max_candidates": 30,
        "elbow_detection": true,
        "min_relevance_threshold": 60,
        "skill_fetch_instructions": ""
      },
      "enabledTools": [
        "check_context",
        "get_skill",
        "search_skills"
      ],
      "revealSkillContent": false,
      "revealRequiresPurchase": true,
      "recommendedQueries": [
        "Send a reminder for invoice #4523 that's 30 days overdue",
        "Send a polite follow-up for invoice 1047 to client@company.com",
        "Reminder for invoice #8921 with a friendly tone"
      ],
      "license": "MIT"
    },
    "systemPrompt": {
      "content": ""
    },
    "skills": [],
    "groups": [],
    "tools": [
      {
        "name": "send_reminder",
        "description": "Send a payment reminder email or notification for an overdue invoice. Accepts the invoice ID and a tone setting.",
        "httpMethod": "POST",
        "baseUrl": "https://httpbin.org",
        "pathTemplate": "/post",
        "queryParams": {},
        "headers": {
          "Content-Type": "application/json"
        },
        "inputSchema": {
          "type": "object",
          "required": [
            "invoiceId",
            "tone"
          ],
          "properties": {
            "tone": {
              "type": "string",
              "description": "The tone of the reminder message (e.g. 'friendly', 'urgent', 'formal')."
            },
            "invoiceId": {
              "type": "string",
              "description": "The ID of the invoice to send a reminder for."
            }
          }
        },
        "outputMapping": {},
        "artifactTemplate": null,
        "authMethod": "user_managed",
        "creditCost": "0.5000",
        "timeoutMs": 30000,
        "userConnectionSlot": "billing_api",
        "userConnectionSecretTemplate": null,
        "routes": [
          {
            "slug": "default",
            "description": "Send a payment reminder email or notification for an overdue invoice. Accepts the invoice ID and a tone setting.",
            "httpMethod": "POST",
            "pathTemplate": "/post",
            "paramsSchema": {
              "type": "object",
              "required": [
                "invoiceId",
                "tone"
              ],
              "properties": {
                "tone": {
                  "type": "string",
                  "description": "The tone of the reminder message (e.g. 'friendly', 'urgent', 'formal')."
                },
                "invoiceId": {
                  "type": "string",
                  "description": "The ID of the invoice to send a reminder for."
                }
              }
            },
            "outputMapping": {},
            "artifactTemplate": null,
            "fieldTransforms": {},
            "resultContext": ""
          }
        ]
      }
    ]
  },
  "connectionRequirements": [
    {
      "slot": "billing_api",
      "type": "api_key",
      "service": "custom",
      "signupUrl": "https://api.billing.example/api-keys",
      "description": "API key for the billing reminders endpoint. The key will be sent as a header.",
      "displayName": "Billing API",
      "secretSlots": [
        "api_key"
      ],
      "allowedDomains": [
        "httpbin.org"
      ]
    }
  ]
}